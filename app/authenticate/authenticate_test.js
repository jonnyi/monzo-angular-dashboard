'use strict';

describe('myApp.authenticate module', function() {

  beforeEach(module('myApp.authenticate'));

  describe('authenticate controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var authenticate = $controller('AuthenticateCtrl');
      expect(vauthenticateCtrl).toBeDefined();
    }));
  });
});
